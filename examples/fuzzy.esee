changeAxis(0, 0)
goTo(200, 200)
setSize(1)
repeat (360) {
	setColor(getRandomColor())
	line(getLayerWidth() / 2 + getSine(255 * repeatCount) * 150, getLayerWidth() / 2 + getSine(201 * repeatCount) * 150)
}
